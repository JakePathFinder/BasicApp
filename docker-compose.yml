services:
  backend:
    build: ./backend
    container_name: basic-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: basic-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
